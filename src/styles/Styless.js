import { StyleSheet } from "react-native";

export const estilosTu = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
    textAlign: 'center'
  },
  description: {
    fontSize: 20,
    color: '#a9a9a9',
  },
  adress: {
    fontSize: 25,
    color: '#add8e6',
  },
  name: {
    fontSize: 30
  }
});
